// Template variant stub — imported by LineupIntro when template === "world-cup-gold"
// All styling is currently driven by the template prop passed to scenes.
// Add template-specific layout overrides here as needed.
export {};
